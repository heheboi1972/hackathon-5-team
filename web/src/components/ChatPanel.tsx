// 역할: 대화 검색 챗봇 패널 — POST /chat, 인용 표시, 리다이렉트 문구 (참조: FR-006) — 시여 담당
// 스캐폴딩 스텁: useChat mutation·히스토리 관리는 TODO(시여)
export default function ChatPanel({ coupleId }: { coupleId: string }) {
  return <div className="text-sm text-gray-400">ChatPanel TODO — couple {coupleId}</div>;
}
